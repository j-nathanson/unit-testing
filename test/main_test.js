// Test Suite, block of unit test test same function or similar part of code

const { expect } = require("chai");
const { describe } = require("mocha");

// Test Suite
describe(' ', function () {
    // Test Spec (unit test), similar to describe, sanity test
    it('should run our test using npm', function () {
        // ok is truthy
        expect(true).to.be.ok
    })
})